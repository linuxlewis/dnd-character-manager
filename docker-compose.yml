services:
  app:
    build: .
    ports:
      - "${PORT:-4000}:4000"
    volumes:
      - appdata:/app/data
    environment:
      - DATABASE_URL=/app/data/app.db
      - PORT=4000
      - HOST=0.0.0.0
    restart: unless-stopped

volumes:
  appdata:
